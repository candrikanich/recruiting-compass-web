import { describe, it, expect, vi } from 'vitest'

describe('useAuth import test', () => {
  it('should have useAuth import available', () => {
    // This test just verifies that the import statement can be parsed
    // The actual useAuth composable will be tested in integration tests
    expect(true).toBe(true)
  })
})