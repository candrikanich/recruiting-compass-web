/**
 * pages/onboarding/index.vue — real component tests.
 *
 * The previous version of this file (61 tests) was fully tautological —
 * every test body was `expect(true).toBe(true)` describing intended
 * navigation/validation/completion behavior that was never actually mounted
 * or exercised — planning/audit-2026-07-27-findings.md ("6. Testing").
 * Deleted in full; see .superpowers/sdd/task-11-report.md for the real
 * mount-based tests that replace it in the following commit.
 */
import { describe, it } from "vitest";

describe.skip(
  "pages/onboarding/index.vue (real tests land in the next commit)",
  () => {
    it.skip("placeholder removed — see task-11-report.md", () => {});
  },
);
