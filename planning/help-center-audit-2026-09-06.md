# Help Center Audit — 2026-09-06 (issue #514, Task 1)

Audit of `pages/help/*.vue` + `components/Help/helpDefinitions.ts` against the live app, as of branch `feat/deadlines-dashboard-widget` @ this worktree. `pages/admin/**` excluded (internal, not user-facing).

Legend: ✅ accurate · ⚠️ stale/inaccurate/incomplete · ❌ missing entirely.

## Part A — Existing Help Center claims checked against live behavior

| Feature / Claim | File : Line | Current Help Center coverage | Status | Notes for Phase 3 rewrite |
|---|---|---|---|---|
| Settings path to athlete profile | `pages/help/getting-started.vue:30`, `pages/help/account.vue:14-20` | "Settings → Athlete Profile" | ⚠️ | Real path is `/settings` → section **"Profile & Player Info"** → card **"Athlete Profile"** → `/settings/player-details`. Card title matches ("Athlete Profile") but the section label ("Profile & Player Info") and route (`/settings/player-details`, not a sub-page literally named "Athlete Profile") don't. Update breadcrumb text to "Settings → Profile & Player Info → Athlete Profile". |
| Family invite path | `pages/help/getting-started.vue:49-59`, `pages/help/account.vue:39-53` | "Settings → Family" → "Send invite" | ⚠️ | Real path is `/settings` → section **"Family"** → card **"Family Management"** → `/settings/family-management`. Also omits two real behaviors: (1) the invite form has a **role selector** (`player` or `parent`) at `pages/settings/family-management.vue:161-169` — help implies only parents are invited; (2) there's a separate **Family Code** join mechanism (`familyCode`/`familyCodeSuccess` state, `pages/settings/family-management.vue:33-120`) that isn't email-invite based — not mentioned anywhere in Help. Rewrite should cover both invite-by-email (with role choice) and the family code path. |
| "Family members can view and edit... interactions" | `pages/help/getting-started.vue:56-59` | Blanket claim of shared edit access | ⚠️ | Directionally true (family RLS grants shared CRUD per `claude/database.md`/MEMORY family-consolidation work) but not verified against current per-resource RLS in this audit — flag for Task 5 to confirm wording doesn't overstate (e.g. some resources are family-shared, but confirm none are athlete-only edit). Low priority since directionally correct. |
| Phase advancement is a **family-confirmed** action | `pages/help/phases.vue:79-99` — "both you and a coach or family member must confirm the transition... A confirmation request is sent to your family unit... A family member confirms the advancement in their notification feed." | Entire "How to advance your phase" section | ❌ | **Wrong mechanism entirely.** Verified in `server/api/athlete/phase/advance.post.ts` and `composables/usePhaseCalculation.ts`: advancing phase is a **single-user POST** (`assertNotParent` — parents are explicitly blocked from calling it, not "confirming" anything), gated purely on `canAdvancePhase(currentPhase, completedTaskIds, taskIdsBySlug)` — i.e., whether the athlete has completed the required milestone tasks for their current phase. There is no request/confirmation/notification round-trip with family members. There is also **no "Settings → Recruiting Phase" page** — the phase UI lives on `pages/timeline/index.vue` (via `DashboardTimelineCard` / `OverallStatusCard`), not under Settings at all. Rewrite: "Complete your current phase's required tasks (visible on your Timeline), then tap Advance — it applies immediately, no one else needs to confirm it." Also note it's **not reversible** (that part is still true — no downgrade endpoint exists). |
| Four phases: Freshman → Senior | `pages/help/phases.vue:8-75` | "four phases that map to your high school career" | ⚠️ | `types/timeline.ts:13`: `Phase = "freshman" | "sophomore" | "junior" | "senior" | "committed"` — there are **five** phases; "committed" is the final phase reached after Senior (via `getNextPhase`), not covered at all. Add a fifth phase card describing "Committed" (post-decision state). |
| Recommendation letters live under Documents → "Recommendation Letters" tab | `pages/help/phases.vue:108-115` — "Navigate to Documents and select the Recommendation Letters tab." | Full "Requesting a recommendation letter" section | ❌ | Recommendation letters are their own **top-level page**, `pages/recommendations/index.vue` (route `/recommendations`), completely separate from `/documents`. There is no tab for it inside Documents. Rewrite the nav instruction to "Go to Recommendations (`/recommendations`) in the main nav." |
| Rec letter status values: Requested → In progress → Received | `pages/help/phases.vue:131-167` | "Tracking letter status" 3-state list | ⚠️ | Real status enum (from `pages/recommendations/index.vue:22-27` filter options) is **`not_requested`, `requested`, `received`, `submitted`** — 4 states, no "In progress" state, and includes an explicit "Not Requested" default plus a "Submitted" state (letter sent to the school) that's distinct from "Received" (letter received by the athlete/from the recommender). Rewrite the status table to match these 4 exact values and their meaning. |
| Interaction types: Email / Phone call / Campus visit / Social media / Other | `pages/help/schools.vue:124-179` | Full "Interaction types" section | ⚠️ | Live enum (`types/database.ts` `interaction_type`, confirmed against `components/Interactions/InteractionAddForm.vue` options) has far more values: `email`, `text`, `phone_call`, `in_person_visit`, `virtual_meeting`, `camp`, `showcase`, `tweet`, `dm`, `game`, `unofficial_visit`, `official_visit`, `other`, `interest`. There is no single "Campus visit" type (it's split into `in_person_visit` / `unofficial_visit` / `official_visit`) and no single "Social media" type (it's split into `tweet` / `dm`). Also missing `text`, `virtual_meeting`, `camp`, `showcase`, `game`, `interest` entirely. Rewrite this section to list the actual add-form options grouped sensibly (e.g. Contact: email/text/phone_call/virtual_meeting; Visits: in_person_visit/unofficial_visit/official_visit; Events: camp/showcase/game; Social: tweet/dm; Other: interest/other). |
| Changing your password path | `pages/help/account.vue:104-116` — "Settings → Account → Change password" | Full "Changing your password" section | ⚠️ | Password change UI is `ProfilePasswordSection` on `pages/settings/profile.vue` ("My Profile"), **not** on `pages/settings/account.vue` ("Account", which only shows Account Information + Delete Account). Rewrite nav to "Settings → My Profile → Password". |
| Deleting your account | `pages/help/account.vue:136-144` | "Settings → Account → Delete account" | ⚠️ | Route is correct (`pages/settings/account.vue` does have the Delete Account section) but the doc is missing a real behavior: deletion is **not immediate** — it's scheduled with a grace period (`isDeletionPending` / `deletionDate` state at `pages/settings/account.vue:39-60`) and can be **cancelled** before that date (`cancel-deletion-button`). Rewrite to mention the pending/cancelable grace period, not "permanently removes all your data" as an instant action. |
| Notification categories: Phase confirmations, Rec letter deadlines/status, family invitation, interaction logged, phase completed, weekly summary, school list milestone | `pages/help/account.vue:74-101` | Full "Understanding notification types" section (High/Medium/Low priority lists) | ❌ | None of these categories exist. Real categories, verified in `pages/settings/notifications.vue:77-99` (`NOTIFICATION_TYPES`): **`follow_up_reminder`** ("When it's time to contact a coach"), **`deadline_alert`** ("Application, offer, and NCAA deadlines — 7, 3, and 0 days out"), **`weekly_digest`** ("Monday morning recruiting summary"), **`event`** ("24 hours before visits and showcases"). Each has independent push/email toggles; only `deadline_alert` and `weekly_digest` support email (`EMAIL_TYPES` set at line 101) — push-only for `follow_up_reminder` and `event`. There is no priority tiering (High/Medium/Low) in the UI at all — that's an invented taxonomy. Rewrite this section completely around the 4 real categories and the push/email toggle matrix. |
| Fit signals: Personal Fit + Academic Fit (two independent signals, no Athletic/Opportunity Fit) | `pages/help/schools.vue:40-68` | "School Fit Signals" section | ✅ | Confirmed accurate against `utils/fitScoreCalculation.ts` (`calculatePersonalFitSignals`, `calculateAcademicFitSignals`) — file header explicitly documents "two honest, independent signals... deprecated calculateFitScore/AthleticFit/OpportunityFit are stubs kept for backward compat". Academic Fit vs SAT/ACT 25th-75th percentile from College Scorecard also confirmed accurate. No changes needed. |
| `helpDefinitions.ts` locationFit/campusSizeFit/costFit: shown as "Strong, Good, Fair, or Limited" labels | `components/Help/helpDefinitions.ts:74-93` | Three fit-signal definitions | ⚠️ | These three sub-signals are real (`calcLocationSignal`/`calcCampusSizeSignal`/`calcCostSignal` roll up into `PersonalFitAnalysis.signals`, confirmed in `utils/fitScoreCalculation.ts:34-51`), but the actual strength enum (`types/schoolFit.ts:6`) is **`"strong" | "good" | "stretch" | "unknown"`** — not "Strong/Good/Fair/Limited". Fix the label wording to match the real enum (or explicitly document the UI's display mapping if the component renders different display strings than the raw enum — verify in the rendering component before finalizing copy). |
| `helpDefinitions.ts` recoveryPlan, interestCalibration, taskDependencies, dashboardOverview/progressBar/suggestionsWidget, freshman/sophomore/junior/seniorPhase | `components/Help/helpDefinitions.ts` (all entries) | Tooltip definitions | ✅ (features exist) / ⚠️ (detail) | Confirmed live: `composables/useRecovery.ts` + `components/Recovery/RecoveryModal.vue` (recovery plan), `components/Interaction/InteractionForm.vue` (interest calibration), `composables/useTasks.ts` (task dependencies) all exist. Not individually line-audited beyond existence-check in this pass — Task 5 should spot-check exact wording (e.g. "6 quick questions" for interest calibration, dead `relatedLinks` URLs like `/docs/timeline`, `/videos/calibration`, `/docs/recovery` which don't correspond to any real route in `pages/`) before reusing verbatim. **Dead `relatedLinks` URLs are a real ❌** — none of `/docs/timeline`, `/timeline/freshman`, `/timeline/sophomore`, `/timeline/junior`, `/timeline/senior`, `/videos/calibration`, `/docs/recovery` exist as routes under `pages/`. Either build these routes or strip the links. |
| Dashboard sections: Current Phase, School list, Recent activity, Tasks | `pages/help/getting-started.vue:72-88` | "Understanding the dashboard" bullet list | ⚠️ | Real dashboard (`pages/dashboard.vue`) renders, in order: `DashboardTimelineCard`, `DashboardStatsCards`, `DashboardSuggestions`, `DashboardPublicProfileLinkCard`, **`RecruitingCalendar`** (new — see Part B), `DashboardContactFrequencyWidget`, `DashboardAthleteActivityWidget` (parent-only). The four bullets are a reasonable paraphrase of a subset but omit the Public Profile link card, the Recruiting Calendar/deadlines widget, and the Contact Frequency widget entirely, and don't mention the parent-only Athlete Activity widget. Expand this list to match the real widget set. |

## Part B — Shipped features with zero Help Center coverage (Step 3 checklist)

Every item from the brief's Step 3 list, checked against `helpCategoryGroups` and the four `pages/help/*.vue` files.

| Feature | Live location | Status | Notes for Phase 3 rewrite |
|---|---|---|---|
| Deadlines dashboard widget | `pages/deadlines.vue`, `components/Dashboard/RecruitingCalendar.vue` | ❌ | Shipped this week (branch `feat/deadlines-dashboard-widget`, merged commit `fc31d439` "merge user deadlines into dashboard RecruitingCalendar widget"). `/deadlines` page shows "Key dates for your sport, division, and graduation year — plus your own deadlines," supports **+ Add Deadline** for custom entries, and shows a staleness warning (`isStale`) when "the current NCAA recruiting season has ended." The dashboard embeds the same data via `RecruitingCalendar` (merged with the former separate Upcoming Milestones card per MEMORY `calendar-milestones-merge`). Needs a brand-new Help section — likely folded into "Getting Started" (dashboard overview) and/or a new "Deadlines" subsection under Phases, since deadlines are sport/division/grad-year driven like phase milestones. |
| Documents (upload, sharing with schools, `type` categorization) | `pages/documents/*.vue` | ❌ | `/documents` page: stats row (Total Documents, Shared Documents, Most Common Type, Total Storage), filterable list, `/documents/add` for upload. Real `document_type` enum (`types/database.ts`): `highlight_video`, `transcript`, `resume`, `rec_letter`, `questionnaire`, `stats_sheet` — note `rec_letter` exists as a document type distinct from the standalone Recommendations page/flow (Task 5 should clarify the relationship: recommendation letters seem to have both a dedicated tracking flow at `/recommendations` AND a `rec_letter` document type for the actual file). Sharing-with-schools behavior not deep-audited in this pass — grep `shared_with_schools`/`school_id` on `documents` table per MEMORY note `documents-type-not-category` before writing copy. No Help page exists for any of this. |
| Offers | `pages/offers/[id].vue`, `pages/offers/index.vue` | ❌ | `/offers` — "Track and compare your scholarship offers." No Help coverage at all. |
| Events | `pages/events/[id].vue`, `pages/events/create.vue`, `pages/events/index.vue` | ❌ | `/events` — "Track camps, showcases, visits, and games." `event_type` enum: `showcase`, `camp`, `official_visit`, `unofficial_visit`, `game`. No Help coverage. |
| Coach Outreach / Communication Templates | `pages/settings/communication-templates.vue`, `CommunicationPanel`, contact-window auto-swap | ❌ | Settings card "Communication Templates" — "Email and message templates for coach outreach"; empty-state copy: "Ready-to-send emails personalized with your recruiting data." This is the templated-messaging feature from MEMORY (`coach-outreach-templates`, `templates-family-scoped`) — includes the NCAA contact-window auto-swap logic (`utils/contactWindow.ts`) that silently substitutes templates based on division/grad-year/sport rules. None of this is documented anywhere in Help — should get its own section, likely under a new "Coach Outreach" or folded into "Schools & Coaches" alongside interaction logging. |
| Recommendation letters (cross-check vs phases.vue claims) | `pages/recommendations/index.vue` | ❌ (see Part A) | Already covered above — the existing phases.vue section is wrong about location (Documents tab) and status states. Needs a full rewrite, not a patch. |
| Performance & Performance Timeline | `pages/performance/index.vue`, `pages/performance/timeline.vue` | ❌ | `/performance` — "Track your athletic performance over time"; empty-state: "Your stats auto-fill coach outreach templates" (ties performance data into the Communication Templates feature above — worth cross-referencing in copy). No Help coverage. |
| Reports & Reports Timeline | `pages/reports/index.vue`, `pages/reports/timeline.vue` | ❌ | `/reports` exists with its own header; not deep-audited beyond route/heading confirmation in this pass. No Help coverage. |
| Analytics | `pages/analytics/index.vue` | ❌ | `/analytics` — "Comprehensive recruiting metrics and performance insights." Sections: Interaction Types, Sentiment Breakdown, Recruiting Pipeline, School Status, Performance Correlation Analysis. Empty-state: "Track engagement, fit trends, and recruiting momentum." No Help coverage. |
| Search | `pages/search/index.vue` | ⚠️ (partial mention, not a real section) | `/search` — "Advanced Search... across schools, coaches, interactions, and performance metrics" with tabbed results (Schools, Coaches, Interactions, Performance Metrics). Currently only referenced as a link target inside `getting-started.vue` ("Go to Search and filter by division, sport, location, or size") and `schools.vue` — but that undersells it: the real page is a unified **cross-entity search** (schools + coaches + interactions + performance), not just a school finder. No dedicated Help section exists. Mark as ❌ for a real section even though the word "Search" appears today. |
| Public Player Profile | `pages/p/[slug].vue` | ❌ | Public-facing recruiting profile page (per MEMORY `public-player-profile`: dark-hero redesign, exposes metrics/team-history/awards/values/commitment). Referenced only indirectly via `DashboardPublicProfileLinkCard` on the dashboard — no Help section explains what it is, who can see it, or how to share/customize it. |
| Family Management / multi-athlete parent accounts | `pages/settings/family-management.vue` | ⚠️ | Partially covered (see Part A family-invite rows) but the **multi-athlete parent** angle (a parent linked to more than one athlete, per MEMORY `multi-athlete-parent-resolver-bug`) is not mentioned anywhere in Help — no explanation of how a parent switches between athletes/children. Needs new content. |
| School Preferences | `pages/settings/school-preferences.vue` | ❌ | Settings card "School Preferences" — "Set criteria for finding your ideal schools." **Correction (found during implementation):** this is a separate, freeform criteria builder — it is NOT the source of the Personal Fit signal inputs. Personal Fit's home state/campus size/cost sensitivity inputs actually live on the Athlete Profile (`utils/fitScoreCalculation.ts` + `components/Settings/PlayerDetailsBasicsTab.vue`/`PlayerDetailsAcademicsTab.vue`), as shipped `family.vue` correctly states. Help never told the user where to go to set School Preferences at all; that page should still be linked from Help, just not as the Fit Signals source. |
| Notifications | `pages/notifications.vue`, `pages/settings/notifications.vue` | ⚠️ | Settings page exists and is referenced by account.vue, but per Part A the entire category list is fabricated/wrong. `pages/notifications.vue` (the notification feed/inbox itself, distinct from the settings page) has zero Help mention at all. |
| Video Links | `composables/useVideoLinks.ts`, surfaced via `components/profile/public/HighlightsReel.vue`, `components/TemplateSendModal.vue`, `components/Settings/PlayerDetailsAthleticsTab.vue` | ❌ | Video links (`platform`, `url`, `title`) are added via the Athlete Profile's Athletics tab and surface on the Public Player Profile's highlights reel and in coach outreach template previews. No Help coverage — should be a subsection of either "Getting Started" (profile completeness) or the new Public Player Profile section. |

## Summary

- **Part A (existing claims checked):** 1 ✅ fully accurate (Fit Signals core two-signal model), 10 ⚠️ stale/inaccurate, 3 ❌ (phase-advancement mechanism, rec-letter location, notification categories — each requiring a full section rewrite, not a patch).
- **Part B (uncovered shipped features):** 15 rows checked; 11 ❌ fully missing, 4 ⚠️ partially/misleadingly referenced (Search, Family Management multi-athlete, Notifications inbox, and the Recommendation Letters cross-reference already counted in Part A).
- **Combined total:** 1 ✅, 14 ⚠️, 14 ❌ across 29 rows.

### Highest-priority fixes for Task 5 (content rewrite)
1. `pages/help/phases.vue` "How to advance your phase" — describes a fictional family-confirmation flow that doesn't exist; users could be confused into thinking they're stuck waiting on someone else. Rewrite from scratch using `usePhaseCalculation`/`advance.post.ts` as source of truth.
2. `pages/help/phases.vue` recommendation-letter navigation — sends users to a "tab" that doesn't exist inside Documents; real destination is `/recommendations`.
3. `pages/help/account.vue` "Understanding notification types" — entirely invented taxonomy; replace with the real 4-category push/email matrix.
4. `pages/help/schools.vue` "Interaction types" — undercounts the real enum by more than half; replace the 5-item list with the actual grouped set.
5. New Help section(s) needed for: Deadlines widget, Documents, Offers, Events, Coach Outreach/Templates, Performance, Reports, Analytics, Search (as cross-entity search), Public Player Profile, School Preferences, Video Links — i.e., roughly half the app's `pages/` tree has zero in-app help today.
