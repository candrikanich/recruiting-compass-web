name: Test Deployment

on:
  push:
    branches: [develop]
    paths:
      - ".github/workflows/test-deploy.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ Workflow triggered successfully"
      - run: echo "If you see this, the workflow system is working!"
