# DEPRECATED - Use deploy-staging-manual.yml instead
# This file exists only to clear the GitHub Actions cache of the old workflow
name: Deploy to Staging

on:
  workflow_run:
    workflows: ["Test & Verify"]
    types: [completed]
    branches: [develop]

jobs:
  noop:
    name: Deprecated - Do not use
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is deprecated. Use 'Deploy Staging Manual' from Actions tab."
